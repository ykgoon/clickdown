## 1. URL Parser Module

- [x] 1.1 Create `src/utils/url_parser.rs` with `ParsedUrl` enum covering all 7 resource types (workspace, space, folder, list, task, comment, document)
- [x] 1.2 Implement `UrlParser::parse(&str) -> Result<ParsedUrl, ParseError>` supporting long-form URLs (workspace, space, folder, list)
- [x] 1.3 Implement short-form URL parsing for tasks (`/t/{id}`), documents (`/d/{id}`), and comments (`/t/{id}?comment={id}`)
- [x] 1.4 Handle edge cases: trailing slashes, query parameters, non-ClickUp domains, malformed input
- [x] 1.5 Add unit tests for round-trip symmetry: every URL generated by `ClickUpUrlGenerator` must parse back to the same resource IDs

## 2. Keyboard Shortcut Chord Support

- [x] 2.1 Add `pending_key: Option<KeyCode>` field to `TuiApp` for tracking leader key state
- [x] 2.2 Handle `g` key in `update()` — set pending state instead of immediate action
- [x] 2.3 When pending and `u` is pressed, open URL input dialog; otherwise pass second key through to screen handler
- [x] 2.4 Add timeout or `Esc` handling to clear pending state
- [x] 2.5 Ensure `g` leader key does not interfere with existing `g` usage (if any) in screen-specific handlers

## 3. URL Input Dialog Widget

- [x] 3.1 Add `DialogType::UrlInput` variant to the existing `DialogState` enum
- [x] 3.2 Add `url_input_text: String` and `url_input_error: Option<String>` fields to `TuiApp`
- [x] 3.3 Implement dialog input routing: capture typed characters, `Ctrl+V` paste, `Enter` submit, `Esc` cancel
- [x] 3.4 Render the URL input dialog with prompt, text field, and inline error display using existing `render_dialog` infrastructure
- [x] 3.5 Add empty-submission validation: show "Please enter a URL" inline error without closing dialog

## 4. URL Navigation Logic

- [x] 4.1 Add `fn navigate_from_url(&mut self, url: &str)` method on `TuiApp`
- [x] 4.2 Parse the URL and match on `ParsedUrl` variant to determine target resource type
- [x] 4.3 Implement workspace/space/folder/list navigation: use existing `navigate_into` flow with IDs from parsed URL
- [x] 4.4 Implement task navigation: fetch task via API (`GET /task/{id}`), extract workspace/list context from response, navigate through hierarchy
- [x] 4.5 Implement comment navigation: navigate to task detail, load comments, highlight target comment
- [x] 4.6 Implement document navigation: fetch document via API, open document view
- [x] 4.7 Handle "already at target" case: refresh current view instead of error

## 5. Error Handling and Loading Indicators

- [x] 5.1 Add `navigating: bool` and `navigating_level: String` fields to `TuiApp` for loading state during sequential navigation
- [x] 5.2 Update status bar to show "Loading... <level>" during multi-level navigation
- [x] 5.3 Handle API 404 errors: close dialog, show "Resource not found" in status bar, restore previous screen
- [x] 5.4 Handle network errors: close dialog, show "Navigation failed: <error>" in status bar
- [x] 5.5 Handle unauthenticated state: prevent dialog from opening, show "Please authenticate first"
- [x] 5.6 Handle insufficient context for short-form URLs: show "Cannot navigate: insufficient resource context"

## 6. Help Dialog Update

- [x] 6.1 Add `g` `u` shortcut entry to the help dialog's Navigation section in `render_help()`
- [x] 6.2 Verify all existing help dialog categories are preserved

## 7. Integration Testing

- [x] 7.1 Add unit tests for `UrlParser` covering all URL patterns from the spec (10+ scenarios)
- [x] 7.2 Add integration test: parse URL → navigate → verify screen state matches expected target
- [x] 7.3 Add integration test: invalid URL → verify error displayed and previous screen restored
- [x] 7.4 Verify `cargo test` passes with all existing tests unchanged
