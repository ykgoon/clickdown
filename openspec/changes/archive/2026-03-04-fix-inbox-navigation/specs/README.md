## No Spec Changes Required

This change is a bug fix for existing navigation behavior. No new requirements are added and no existing requirements are modified.

The navigation behavior (Enter on sidebar item navigates to that destination) is already covered by existing specs under `tui-navigation/spec.md`. This fix ensures the Inbox item in the sidebar properly triggers navigation to the Inbox screen, which is the intended behavior.
